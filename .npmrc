# pnpm: don't run a deps-status check before scripts (breaks nested builds
# like OpenNext's `pnpm build`); and don't fail on optional native build scripts.
verify-deps-before-run=false
strict-peer-dependencies=false
node-linker=hoisted
